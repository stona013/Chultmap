// Optional: Supabase für gemeinsame Marker aktivieren.
// Wenn diese Werte leer bleiben, verwendet die Seite nur localStorage
// im jeweiligen Browser.

window.APP_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
